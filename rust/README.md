# lasio_rust

This crate provides experimental Rust functionality for `lasio` using PyO3.
Currently only the function `get_version()` is implemented.

To build the extension run:

```bash
pip install maturin
maturin develop
```

